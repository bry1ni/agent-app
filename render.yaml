services:
  - type: web
    name: agent-app
    env: python
    buildCommand: pip install -e .
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT --reload
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0 